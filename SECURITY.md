# Security policy

## Reporting a vulnerability

Email: smigolsmigol@protonmail.com

Acknowledgment SLA: 48 hours.
Critical-fix SLA: 7 days.

## Supported versions

| Version | Supported |
|---------|-----------|
| latest  | Yes       |

## Default for the f3d1 ecosystem

This file is the org-level default. Every f3d1 repo that does not ship its own `SECURITY.md` inherits this policy automatically (Github auto-uses files in the `.github` magic repo as defaults).

For runtime-specific concerns on a particular package, the relevant repo's `SECURITY.md` (when present) overrides this file.

## Scope

In scope: any of the actively-maintained f3d1 repos (f3dx, llmkit, f3dx-cache, pydantic-cal, f3dx-router, f3dx-bench, tracewright, keyguard, .github).

Out of scope: forks, archived repos, third-party dependencies (file with the upstream).
